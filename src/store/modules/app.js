
const state = {
  searchValue:''
}

const actions = {

}

const mutations = {
  SET_SEARCH (state, searchValue) {
    state.searchValue=searchValue
  }
}

export default {
  state,
  actions,
  mutations
}
